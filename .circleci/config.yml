
version: 2.1
orbs:
  aws-cli: circleci/aws-cli@1.1.0
  aws-s3: circleci/aws-s3@1.0.15
jobs:
  build:
    working_directory: ~/project
    docker:
      - image: cimg/python:3.6
    steps:
      - checkout
      # - run:
      #     name: Setting env/qa branch environment variables.
      #     command: |
      #       mkdir ~/.aws
      #       echo -e "[default]\naws_access_key_id=$AWS_ACCESS_KEY_ID_CIRCLECI\naws_secret_access_key=$AWS_SECRET_ACCESS_KEY_CIRCLECI\n" >> ~/.aws/credentials
      #       echo -e "[default]\nregion=us-east-1\noutput=text\n" >> ~/.aws/config
      #       echo -e "[user]\naws_access_key_id=$AWS_ACCESS_KEY_ID_CARO\naws_secret_access_key=$AWS_SECRET_ACCESS_KEY_CARO\n" >> ~/.aws/credentials
      #       echo -e "[user]\nregion=us-east-1\noutput=text\n" >> ~/.aws/config
      - run:
          name: Set AWS Credentials
          command: |
            echo 'aws configure --profile default set aws_access_key_id $AWS_ACCESS_KEY_ID_CIRCLECI'
            echo 'aws configure --profile default set aws_secret_access_key $AWS_SECRET_ACCESS_KEY_CIRCLECI'
            echo 'aws configure --profile default set region us-east-1'
            echo 'aws configure --profile default list'
      - run:
          name: Set AWS Credentials
          command: |
            echo 'aws configure --profile user set aws_access_key_id $AWS_ACCESS_KEY_ID_CARO'
            echo 'aws configure --profile user set aws_secret_access_key $TEST_AWS_SECRET_ACCESS_CARO'
            echo 'aws configure --profile user set region us-east-1'
            echo 'aws configure --profile user list'
      - aws-cli/setup:
          arguments: '--profile user'
      - aws-s3/copy:
          from: testuploads3_carolina.txt
          to: s3://circlecitestupload
